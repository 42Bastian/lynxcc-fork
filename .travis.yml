language: c
# Mirrors .github/workflows/ci.yml: build the toolchain, libraries and
# examples, then run the test suite. GearLynx is not installed here, so the
# integration tests skip and the host unit tests are the gate.
script:
  - make bin
  - make lib
  - make examples
  - make tests
